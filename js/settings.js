var SETTINGS = (function ( ) {

  // Public Variables
  var SE = { };

  // Story Settings
  SE.speedy      = false;
  SE.waitSeconds = 1;

  // Private Variables

  // var privateVariable = 1;
    
  // Public Functions
  // SE.init = function ( ) {
    
  // };


  // Private Functions
  


  return SE;
}( ));
